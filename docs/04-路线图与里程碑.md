# 04 · 路线图与里程碑（长期任务清单）

> 原则：**每次迭代只带一个主目标 + 若干小修**；按"真实反馈驱动"排序（哪痛先治哪）。
> 每里程碑验收 = smoke 全绿 + 对应真实回归/场景复现 + 文档同步。

## M1（进行中/大半完成）· 门禁语义补完 —— C2/C3/audit/D4
- [x] C3 出界分级声明制（expectedOutOfSafeArea；回滚 decoration 豁免出界）（v0.3.2）
- [x] C1 自动回退链（v0.3.1）｜[x] C2 audit 禁 autoDeclare（v0.3.1）｜[x] D4 导入安全区探测（v0.3.2）
- [x] audit 导出回读断言（页数/尺寸/最小字号）+ 审计提示词铁律（v0.3.2）
- [ ] 验收：audit 档用 19 页 deck 实测（何时 deck 可用再跑）。
- 注：完整"证据链（数据来源核查）"归 M3，audit 第一版不做。

## M2 · 文本实测档（渲染回读测量）—— 解决 D4 的"估算被样式骗过"（v0.12.0 完成）
- [x] 设计：headless 测量通道（页面内 JS + Range 行盒 → `#__ppt_measured` → Edge dump-dom）→ `preview/measured.json`（独立档，不污染 layout.json 契约）。
- [x] **D3 决策落地**：实测=终审、估算=预检——`ppt_verify measured=true`：实测溢出且估算未报 = error（估算漏报，M2 核心捕获）；实测复现 = warning；估算过报 = relief warning；无浏览器 → 降级估算 + 标注"未实测"。
- [x] 性能：100 页基准（render 70-406ms / measure 220-320s → 并发4+独立 profile 2.2s/页）→ 技术报告 §6。
- [ ] 导出 auto-fit 参考实测档（后续：实测与估算同源 HTML 渲染 vs PPTD 链路一致，收益评估后决策——保留估算门槛防浏览器字体缺失差异）。
- 注：验收"漏报样例"以行数差异捕获实证（文本框 5：估算 1 行 vs 实测 6 行——真实断行差异）。

## M2.5 · 出界 bleed（C11）按需
- [ ] 仅当真实任务出现有意出血设计时：讨论 `expectedOutOfPage` 或在设计规范中明确"出血=安全区级声明"；当前超页=绝对 ERROR。【待议，勿提前做】

## M3 · 整体性与数据连贯（设计决策 6.5 的实现，v0.13.0 完成）
- [x] 跨页对账：`ppt_crosscheck`（同数字/百分比跨页 → 页号+上下文清单；单页数字不入组；cap 30）——"同一指标多页值不一致"发现机制（建议级）。
- [x] 证据状态机 v1：页面 `source` 标注 → grounded/unmapped，交付说明"数据来源核查表"（工作流铁律 7）。
- [x] 总览图：`ppt_shot overview=true`（deck.html 整览 PNG，N 页纵排，>30 截前 30）——跨页节奏视觉审阅。
- [x] theme token 符合性断言：`$ref`（fill/color/style/stop/background/theme 内）缺失 → resolveDeck error（防字面 `$xxx` 上屏静默回退；正确引用零影响）。
- [ ] 证据状态机 v2（candidate/conflicted/missing）：需来源对比能力（对用户资料做语义级匹配），待真实多源任务驱动。

## M4 · 体验与预览
- [x] 导入保样式（v0.4.0，P0-1）→ import 工程预览基本还原原稿视觉
- [x] **pptx 产物直接截图**（v0.8.0 Office COM 真渲染通道：audit 档导出自动渲染 + ppt_visual；无 Office 自动降级并标注）
- [x] 性能基准（v0.12.0：100 页 render/measure 实测进 §6；截图复用独立 profile 已在前；大媒体流式写入/scale 探测仍留待）
- [ ] dsh 网页 client 内嵌预览面板（渲染器复用，对话内翻页/总览）——需要 client 插件基建（Slot UI + client 构建链），列为下一独立批次；现有 ppt_preview 同源链接 + 整览 PNG 已覆盖对话内看稿。
- [ ] `pptxgenjs` 第三引擎（**D6 已定：发布与自动回退链演进等 v1.0 再动**）。
- [ ] 大媒体流式写入 / scale 上限探测（Edge 超大截图；overview 已自动截 30）。

## M5 · 分发与生态
- [ ] **D6 已定**：v1.0 前保持本地注入器闭环；评测集（四类任务 + 复杂场景陷阱样例 + 自动评分）过审后 GitHub Release + npm 包。
- [ ] 预设目录出厂化（`agent presets copy` 流程文档化）。

## M6 · 内置制作技能（2026-09-14 完成 —— 用户诉求"skills 能不能提升制作能力"）
> 用户硬约束：**技能只是锦上添花，绝不能影响既有工作流；老用户更新后用法零变化**。本轮全部设计以此为准。
- [x] **调研**（借鉴不采用）：读透 humanizer / humanizer-zh / nature-paper2ppt / skill-creator / writing-skills → 条目骨架「信号→诊断→改法（含 before/after）+ 成组才判」、description 只写触发、正文短。
- [x] **三本自研技能**：`ppt-studio-craft`（叙事与版式）/ `ppt-studio-data`（图表与数据）/ `ppt-studio-copy`（中文文案与去 AI 味），与原有 `ppt-studio-manual` 并列。
- [x] **接入**：`src/skill.js` 扫 `skills/*/SKILL.md` 逐本内嵌注册（`ctx.skills.register`，落 PPT 预设层）；工作流**只加一行**"何时加载"指引（标准档，quick 档逐字节不变）；`install.mjs` 镜像全部技能。
- [x] **"老用户不受影响"机器断言**：工作流段**逐行超集**（基线 `scripts/fixtures/workflow-baseline.json`，10 变体）+ quick 档逐字节不变 + 技能包结构/长度/优先级边界断言 + 写死数值扫描（5 条校准正则）。
- [x] **对照评测**（方法见 docs/06 §7；打分器 `scripts/eval-skills-score.mjs` 随包提供）：同一 brief、同模型、两轮有效对照——B 臂两轮文本都更省（1202→1073 / 1524→860）、建议项一轮更少一轮持平、无任何指标回退；并在 B 中把"行业报告显示"改写成页内来源提醒 + `source:` 标注"暂不可引用"。**如实结论：不回退 + 更精简 + 一处定性改进**（门禁错误两臂皆为 0，差异不在"过不过门禁"）。
- [x] **结构化盲评**（协议 + `scripts/eval-skills-blind.mjs` 随包提供，见 docs/06 §7.6）：匿名化 + 随机分配后由独立评审员按 6 维打分——**含技能那份 26/30 vs 无技能 22/30**，独立结论"更适合直接交付的是含技能那份"；关键证据命中三本技能的原定目标（标题 5/5 结论句、文案 5/5 无 AI 腔、数据页主动标注"口径不可比/来源未注"），并抓出无技能那份**夹了一句材料里没有的结论**。
- ~~下一轮候选（盲评暴露）：`ppt_verify` 对"含 chart 的页缺少图表标签文本"给一条 `[·]` 建议~~ —— **用户判定不成立，已撤销**：
  图表常常就是装饰/示意/背景，本来就不需要标签文本，不该在这上面加机器提示。相应地把 `ppt-studio-data` §0 改成
  "先分清这张图是**证据还是装饰**"的判据（承载数据才要求标签四件齐；装饰性图表允许无标签，但交付说明标注"仅作示意"、
  且页面结论不得依赖它）。**不做机器提示**，避免把风格偏好变成门禁。
- [x] **独立评审**：交独立 agent 逐条对源码评审，抓出 7 类真实缺陷（变相写死数值、不可执行的 table 右对齐、被扩大的出界声明语义、缺失的降级路径、三本自检重复、标题写法归属不清、字号/图片主线缺失）并全部修补。
- [x] smoke 169 → 177 → **180**（末次为"借鉴经典 skill"一轮 + 3 条文档完整性断言）/ preflight 11 / e2e-1.0 13 / 预设自检 0 漂移；发行产物真进程验证 4 本技能 `registered:true` 且 `visible: runtime/runtime/len=N`。
- [ ] 下一轮候选：**版式配方做成模板母版**（机器可验证的几何，比散文技能更硬）；**技能触发率评估**（本轮只评了效果，没测"该加载时是否真加载"——可借 skill-creator 的触发率方法）。
- 注：技能效果属"软收益"，评测为 n=2 单一 brief 的**方向性证据**，不是统计结论（docs/06 §7.3 已如实标注）。

## 常备维护（每轮）
- 反馈 → 1 个 ★ 项 + 最多 3 个小修；文档同步（01/02/03）；smoke 增量断言；真实回归（19 页 deck + WPS fixture）。
- 每轮结束更新本文件的勾选状态与"下一轮建议"。

## 模板保真候选（fidelity-report.md 延伸；用户定优先级）
- [x] **候选 A（v0.9.1 完成）**：常见 prst 形状直通（17 种：箭头/多边形/星/流程图）+ 渐变双 stop 闭环（schema/import/render/export/verify 五端）；export→import 回环验证 111 断言；真实模板 upArrow/roundRect/ellipse 直通实测。**注**：简约商务原稿 gradFill=0（"渐变观感"实为 custGeom 弧带+半透明，97 个自定义几何），渐变闭环正确未误触发。
- [x] **候选 B 手术模式（v0.10.0 完成）**：`ppt_patch`——模板真身贴内容（文本/表格槽替换，只改 <a:t>，rPr/几何/渐变/字体/图片原样；未动页内容 sha256 验证）；真实模板 24 页实测 patched 恰 2 页 / 158 条目一致；工作流 6b 新增"贴模板保真"路线（与双轨互补，常规导出不变）。**v1.1 预留**：rels 增量/资源注入、grpSp 组合内文本、新增页合并（手术 + 渲染混合成品）。
- [x] **候选 C（v0.11.0 完成）**：custGeom 几何闭环（import/render/export 三端直通：moveTo/lnTo/quadBezTo/cubicBezTo/arcTo/close；SVG 弧几何转换）+ alpha 透明度（solid/渐变 stop）+ 非 rect 形状内文本提取（20 个实证）；简约商务 custGeom 提取 95/97（其余为母版层装饰——背景层边界）。**保真缺陷矩阵关闭**（A/B/C 全完成）。
- [ ] **保真矩阵残余（诚实边界，不阻塞）**：母版/布局层装饰与背景（2 个 custGeom）、LOGO 圆环/组合内图片细节、字体资产（模板专属字体回退）——双轨真身参考 + 手术模式覆盖这些场景；字体资产与背景层分析可作独立小项待议。

---

## 评审基线（来自用户测试反馈 3.3）
> 这套工作流在"可验证的 PPT 生产"上是合格的、甚至优秀的；但学习曲线集中在
> "DSL 规则与两套度量的边界"上。如果工具面能把 **schema 自述（✓v0.3）、
> 安全区（✓v0.3）、声明生成（✓v0.3）、度量归一（✓v0.3 保守档；实测档=M2）**
> 做掉，就可以从"熟练工才能用得顺"变成"人人可用"。

→ 四个卡点 v0.3 已解决三个半；**M2 实测档是最后半个**；M3/M4 是"从能用变好用"的纵深。

## 第一轮端到端测试记录（2026-09-05，萨尔浒之战 10 页）
> 测试会话结论（报告 §3.1）：用户链路（模板库→创作→审阅→实测→导出→真渲染→预览）**成熟可靠**；痛点集中在**开发侧自维护链路**与 **autoDeclare 崩溃**。
- [x] **§A P0 真修复**（0.14.6）：autodeclare yaml@2 崩溃（doc.get()=YAMLSeq；首版修复 `raw.toJS?.()` 裸调用仍抛 "A document argument is required"——smoke 30 回归当场捕获后改 `doc.toJS()`）+ smoke 回归 4 断言。
- [x] **§F P1 降噪**（0.14.6）：themeConformance 字号建议 >3 处 → 聚合一条。
- [x] **§G P2 局部校验**（0.14.6）：`ppt_verify pages=`（不改写 layout.json）。
- [x] **§B/§C（开发链）配套**（注入器 0.3.4）：dev_build_plugin 去掉 checkout 前置门槛；dev_reload_package 对非注入器装配包明确诊断；dev_plugin_status 支持过滤；**单装配源定论**（preset 行；注入器 registry 残留清除，junction 保留）。
- [ ] **待重启端到端验证**：PPT 工作区 `ppt_verify autoDeclare=true` 回归（下轮）。
- [ ] 证据状态机 v2 / M4 client 面板 / M5 评测集 —— 按下一轮反馈排期。

## 第二轮端到端测试记录（2026-09-05，AIC5 第15页：生产流程仿真 × 本体论）
> 任务性质：**编辑既有精美 PPT 的某一页**（20 页原稿只换第 15 页、其余逐字节不动、保留母版横幅）。测试会话结论：主流程闭环优秀，**两类核心困难**：①隐性契约（样式必须写在 content 内——schema 不拦曾致 146 条假错误）；②保真交付（无"单页替换/单页导出"能力，被迫手工 zip 手术 + 哈希自证）。交付本身达标（0 错误审阅/64 对声明/Office 真渲染复核/其余页 SHA256 一致），列为高价值正面样本。
- [x] **A3/A9 ★★ 保真交付**（0.15.0）：`ppt_splice`（替换进原稿，保留母版/备注/媒体，其余条目 SHA256 自证）+ `ppt_slice`（单页 + 完整母版体系）；**E2E 真实资产 + Office COM PASS**（380 条目仅 2 变化，splice/slice 均真渲染成功）。
- [x] **A6 ★ 度量修正**：加粗 CJK ×1.06（Office 实际排版与估算的系统差）。
- [x] **B1 ★ 隐性契约拦截**：元素级样式键 → ppt_check 明确报错 + 指引；SCHEMA_REF 正/反例。
- [x] **A2/A7/A8/C3/B2/C1/A4/A5**：缺失媒体自检+占位兜底+python 错误完整化（A2）；导入全量色板（A7）；对比度渐变承载面（A8）；内边距 near-align 豁免（C3）；YAML 解析错误指引（B2）；README 实现/度量速查（C1）；存量溢出→splice 指引（A4）；`ppt_visual pages`（A5）。
- [x] smoke 133 → **139**（全绿）+ 真实回归 2/2。
- [ ] **待重启端到端**：工作区内真实调用 splice/slice/visual pages（下轮验证）。
- [ ] **边界（诚实记录，未承诺）**：EMF 媒体在 pptd 管线的兼容性（D1）；WPS 等其他实现打开 splice/slide 产物（D3）；M2 浏览器档与 Office 档并存为大校验矩阵（D2，建议作为后续度量源选择项）。
