# 01 · 需求与目标（活文档 · 每次迭代先读）

> 本文件回答"我们为什么做这个插件、要做到什么程度、已经走到哪、哪些地方可能打架"。
> 决策源：`docs/00-设计源.md`（537 行设计文档 + 决策记录表）。
> 更新约定：任何改变需求/目标/约束的改动，必须同时更新本文件与 `docs/03-更新日志.md`。

---

## 1. 初始需求（用户原话要点，2026-08 至 2026-09 逐步明确）

| # | 需求 | 形态 |
|---|---|---|
| R1 | 在 DeepSeek Harness 上做一个 **PPT 插件**：agent preset「PPT 工作室」= shipped **standard 预设全量功能** + 一条插件行 | 预设形态 |
| R2 | 用户提出 **PPT 相关任务**时进入**专用 PPT 工作流**；非 PPT 任务行为与标准模式完全一致 | 路由（双通道：语义 + `/ppt` 命令） |
| R3 | **四类任务全兼容**（差异化亮点）：从头（from-scratch）/ 补完（augment）/ 修改（edit）/ 总结（summarize） | 任务形态 |
| R4 | **核心质量要求**（用户原话）："避免元素区块冲突"——即元素互相压盖/出界/溢出必须被防住 | 布局工程 |
| R5 | 复杂场景（长 deck 50+ 页 / 多源内容 / 严谨内容）要能做，且质量要可验证 | 审计门禁 |
| R6 | 用户提供的参考模板/素材要被尊重（风格与模板参考） | 素材/风格 |
| R7 | 交付物 = 可编辑 .pptx（+ 中间层工程），导出引擎可用性探测 | 交付 |
| R8 | 与现有生态（paper2ppt / PaperSpine）独立共存 | 边界 |

## 2. 目标（长期不变）

1. **一个可验证的 PPT 生产工作流**：产出质量由"数字门禁 + 视觉审阅"双轨保证，而不是靠运气。
2. **四类任务同一个中间层（PPTD）**：deck.yaml + pages/*.yaml + media/ 是唯一事实源，引擎可插拔。
3. **迭代友好**：每次改动有回归（smoke 断言集）、有文档（本目录）、有版本（semver + git）。
4. **元目标**：从"熟练工才能用得顺"进化到"人人可用"（用户测试反馈 3.3 的一句话总结）。

## 3. 演进脉络（已完成里程碑 → 目的）

| 版本 | 目的 | 主要内容 | 验证 |
|---|---|---|---|
| v0.1（MVP） | 跑通"中间层 → 渲染/校验/导出/导入"主链路 | pptd schema / render-html / verify / export-pptx / import-pptx / zips / xmljs；路由与命令面；preset 装配 | 初版 smoke |
| v0.2（P2–P12 修复） | 真实文件兼容 + 布局模型从"0 容忍"演进为**设计意图声明制** | 导入 P2 媒体丢失（UTF-16 WPS）/ P5 元素 ID 去重 / P6 截图缓存 / P9 roundRect+rotation / P10 箭头 marker / P12 中文换行禁则；`expectedOverlaps` 声明制、`role: decoration`、`overlapMode: lenient`；美学建议层（8 条非门禁）| 12→21 断言 |
| v0.2.x（Quick + 引擎 auto） | 低 token 快交付 + 明确引擎语义 | `/ppt quick` 语义/命令双触发；`engine auto=pptd` 写死并写入提示词/工具 | 25 断言 |
| v0.3.0（背景 + 本次反馈） | 背景图全链路 + 解决真实任务反馈的 8 个 ★ | 背景 image/solid 渲染/导出(p:bg)/导入三层识别；**safeArea**（D5）；**autoDeclare 一键声明**（D2）；**保守度量 + 导出阈值统一 + minFontSize 下限**（D4/E2）；**out 绝对路径**（E1）；**line bounds 自动推导**（D1）；**ppt_schema/ppt_new**（B2）；对比度/孤字建议（D6/D7） | 47 断言 + 19 页真实 deck 回归 |

**演进主线**（为什么每一步这么做）：
- 先保主链路可跑 → 再用真实文件（WPS）逼出兼容性问题 → 用真实任务（产能 PPT 19 页）逼出**工具摩擦**（声明成本、度量偏差、安全区、自述缺失）→ 每个摩擦做"插件级修复"，**不改用户工作流习惯**（声明制保留，只是给一键生成）。
- 原则：**严格性（错误门禁）不可降**；**使用成本必须降**。两者冲突时优先保严格性，用自动化降低成本。

## 4. 关键设计决策（已定，勿轻易推翻；推翻 = 必须更新本文件）

1. **中间层 = 自定义 PPTD v1**（独立实现，不依赖 Kimi 逆向生态）。
2. **重叠合法性 = 设计意图声明制**（expectedOverlaps 对照），不是元素类型制；**内容互压（content-collision）永远不可豁免**。
3. **双轨审阅**：数字门禁（render→verify，ERROR 清零）+ 视觉审阅（shot + read_image）；估算档已被证明会漏报 → 视觉审阅不可省。
4. **引擎 auto = pptd（主引擎，图表矢量拼绘）**；python-pptx 仅显式指定或 pptd 硬失败时使用。
5. **样式只用 theme token**；中文最小字号 12pt 为工作流铁律（导出缩字下限 = theme.minFontSize，默认 12）。
6. **Aesthetic `[·]` 建议永远不是门禁**（用户要求审美改进不得引入冲突错误）。
7. 产物 = 中间层项目 + .pptx；`1px = 1pt`；尺寸默认 960×540。
8. 预设 = standard 全量 + 插件行；`C:\Users\11867\.dsh\.agent-presets\ppt\`。

## 5. 冲突与待确认清单（第 2 号请求：可能冲突的地方，需讨论拍板）

> 状态图例：🟢 已定无冲突 ｜ 🟡 有张力但已用默认折衷 ｜ 🔴 真冲突，需讨论。

| ID | 冲突/张力 | 双方立场 | 当前实现（缩略） | 我的建议默认 |
|---|---|---|---|---|
| **C1** 🔴 | **引擎自动回退链**：设计文档说"三引擎 + 自动兜底（python 缺失→pptxgenjs）"；实现只做 `auto=pptd`，python-pptx 仅显式指定。若 pptd 硬失败，**没有自动回退**，只有报错。 | 设计：可用性优先（出活优先）/ 实现：质量优先（从不静默降级到表格） | auto=pptd；pptd 失败 → 报原因，提示 `/ppt engine python-pptx` 切 | **默认保持质量优先**：pptd 失败不自动换引擎（避免"图表降级表格"静默发生），报错 + 给一键切换提示。若要自动回退，请说 |
| **C2** 🔴 | **autoDeclare 是否会掩盖真实问题**：一键声明把"卡片上放文字"全部写成预期重叠，模型可能偷懒把**意外重叠**也声明掉，导致门禁失去意义。 | 声明制成本 vs 声明制严格性 | 只处理 warning 级（content-collision 不可声明，是唯一硬底线）；叠着视觉审阅 | **audit 模式禁用 autoDeclare 参数**（标准模式可用）；或在 autoDeclare 报告里列出"声明封面"（哪些页声明了几对）供抽查。待用户定 |
| **C3** 🔴 | **safeArea 与"故意放在页眉/页脚带的元素"冲突**：模板页脚带常被用户放 logo/角标水印，`out-of-page` 会把它判错。 | 安全区=绝对门禁 / 装饰性页脚元素也合法 | decoration 只豁免 overlap，**不豁免 out-of-page** | 建议：加 `outOfSafeArea: allow` 页级开关 或 `role: decoration` 也豁免出界（二选一），否则安全区阈值内必然误判。待用户定 |
| **C4** 🟡 | **保守度量 vs 误报成本**：0.6em 系数让"实际装得下"的文本也可能被 verify 报溢出 → 逼模型放大容器/缩字。 | 防漏报（用户 D4 反馈）vs 防误报 | 偏宽系数 + 统一阈值 | 保持保守；**实测档（渲染回读）上线后改双档并报**（估算=门禁底线，实测=复核），路线图 R1 |
| **C5** 🟡 | **minFontSize 全局 vs 个别小字号**：caption 注释 10pt 在部分页合理，但铁律"中文最小 12pt"。 | 严格（用户 14/12 承诺）vs 灵活 | 主题级 minFontSize（默认 12），导出不会低于它 | 保持主题级设置；如需页级覆盖再加 `minFontSize` 页级字段（未做，待用户定是否需要） |
| **C6** 🟡 | **quality 三档 vs 现状**：state 里有 quick/standard/audit，但 audit 档的门禁（预览 PNG/设计报告/证据链）未实现。 | 设计（audit 硬门禁）vs 实现（只快/标准） | quick + standard 语义已落地；audit=空 | audit 档先降级为"standard + 强制视觉审阅 + 导出回读断言"（下一里程碑），完整证据链再议 |
| **C7** 🟡 | **导入保真度**："内容保真、版式可从新"（已定）vs 真实工程期望"复刻版式"（用户 A2 提到模板研究）。 | 版式可从新 / 复刻模板 | 导入 = 几何近似（roundRect 保留、渐变→纯色、chart→占位） | 保持所定；"复刻"需求走"导入成 deck → 人工/模型按模板重排版"，不做布局逆推自动化 |
| **C8** 🟢 | 总结任务产出形态 | markdown 默认 / docx+pdf 可选 | 现状：总结走通用能力（未特殊化） | 未实现，路线图低优先 |
| **C9** 🟢 | 跨页数据对账 / 证据状态（设计决策 527 行） | 已定"首版基础版" | **未实现** | 路线图 M3（长期），不阻塞 |
| **C10** 🟢 | Quick 模式跳过视觉审阅 vs "质量底线" | 低 token / 质量 | 数字门禁全跑 + 交付标注"未经视觉审阅" | 已定，不改 |

## 6. 更新日志（新增条目见 docs/03-更新日志.md）

- 2026-09-04：v0.3.0 发布（背景全链路 + 8 项反馈修复）；建立本 docs/ 文档体系、git 版本控制、真实回归脚本。
